# Heap 

- 힙 자료구조를 나타내는 키포인트는 완전 이진 트리(a complete binary tree)를 이용한다는 점이다.
- 완전 이진트리는 만약 차일드가 있다면 왼쪽 부터 채워진다. 이 말은 왼쪽 차일드가 없으면 오른쪽 차일드도 없다고 볼 수 있다.

- 힙을 코드로 구현하는 방법은 두가지가 있는데
1. 각각의 노드를 객체로 두어 루트노드 내 왼쪽 차일드 노드객체, 오른쪽 차일드 노드객체로 표현 하거나,
2. 배열로 나타내 BFS로 배열의 요소 순서를 나타내는 방법이 있다.

a브랜치 
 